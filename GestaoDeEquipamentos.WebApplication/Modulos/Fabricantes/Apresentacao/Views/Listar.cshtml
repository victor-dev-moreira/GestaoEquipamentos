@model List<ListarFabricanteViewModel>

@{
    Layout = "_Layout";
    ViewBag.Titulo = "Listagem de Fabricantes";
}

<p>
    <a class="btn btn-primary" asp-action="Cadastrar">
        <i class="bi bi-patch-plus"></i>
        Cadastrar Fabricante
    </a>
</p>

@if (Model.Count == 0)
{
    <div class="alert alert-warning mt-4" role="alert">
        Não há nenhum fabricante registrado.
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-3 mt-4">
        @foreach (ListarFabricanteViewModel fabricante in Model)
        {
            <div class="col">
                <div class="card h-100">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-center mb-2">
                            <h5>@fabricante.Nome</h5>
                            <span class="badge text-bg-secondary">@fabricante.Id</span>
                        </div>

                        <dl class="row mb-2">
                            <dt class="col-sm-4">E-mail</dt>
                            <dd class="col-sm-8">@fabricante.Email</dd>

                            <dt class="col-sm-4">Telefone</dt>
                            <dd class="col-sm-8">@fabricante.Telefone</dd>
                        </dl>
                        <div class="d-flex gap-2 justify-content-end mt-auto">
                            <a class="btn btn-sm btn-outline-primary" title="Editar" asp-action="Editar"
                               asp-route-id="@fabricante.Id">
                                <i class="bi bi-pencil"></i>
                            </a>

                            <a class="btn btn-sm btn-outline-danger" title="Excluir" asp-action="Excluir"
                               asp-route-id="@fabricante.Id">
                                <i class="bi bi-trash3"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}